# Generated by Ansible for {{ ansible_hostname }}.{{ ansible_domain }}.

server {
    listen {{ vmpreso_listen_port }};
    server_name {{ vmpreso_server_name }};

    root {{ vmpreso_root_dir }};
    index index.html index.htm;
    charset utf-8;

    #if ($request_method !~ ^(DELETE|GET|HEAD|POST|PUT)$ ) {
    #    return 444;
    #}

    location / {
        try_files $uri $uri/ =404;
    }
}
